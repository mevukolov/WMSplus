{
  "name": "wmsplus-print-bridge",
  "version": "1.0.0",
  "private": true,
  "description": "Relays queued WMS+ print_jobs rows to the warehouse's thermal printer over USB via the Windows Print Spooler. One instance, always-on, on the Windows 10 PC the printer is plugged into.",
  "main": "index.js",
  "scripts": {
    "start": "node index.js"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.0",
    "dotenv": "^17.4.2"
  }
}
