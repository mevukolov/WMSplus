# Copy to .env and fill in before running. Never commit the real .env.
SUPABASE_URL=https://bgphllmzmlwurfnbagho.supabase.co
SUPABASE_SERVICE_KEY=
# Exact printer name as Windows shows it under "Устройства и принтеры" /
# "Devices and Printers" (e.g. "TSC DA220") -- NOT an IP, this printer is
# USB-attached to this PC.
PRINTER_NAME=
